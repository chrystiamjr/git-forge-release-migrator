{
  "hooks": {
    "PreToolUse": [
      {
        "type": "command",
        "command": "sh -c 'if [ \"${ENABLE_RTK_HOOKS:-0}\" = \"1\" ] && command -v rtk >/dev/null 2>&1; then rtk hook copilot; else exit 0; fi'",
        "cwd": ".",
        "timeout": 5
      }
    ]
  }
}
