part of 'gfrm_app_theme.dart';

final class GfrmShadows {
  const GfrmShadows({this.card = const BoxShadow(color: Color(0x0F000000), blurRadius: 3, offset: Offset(0, 1))});

  final BoxShadow card;
}
